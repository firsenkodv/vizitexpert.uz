APP_NAME="VIZIT EXPERT UZ"
APP_ENV=local
APP_KEY=base64:hIxqsLk+dLINiuKYjB2/5HXdFIBxdDznIUaUlscXmmc=
APP_DEBUG=true
APP_URL=http://vizitexpert-uz.test

# Основное место настройки Tourvisor — админка: «Настройки сайта» -> вкладка «Турвизор».
# Заполненное там поле перекрывает переменную отсюда, пустое — оставляет её.
TOURVISOR_MODE=auto

# Сторонние подключения: счётчики, CDN, карты, виджеты, CRM (config/external.php).
# false — не подключать ничего внешнего. Нужно под VPN: cdn.jsdelivr.net,
# tourvisor.ru, api-maps.yandex.ru, connect.facebook.net, analytics.tiktok.com
# и adminway.ru оттуда недоступны и висят до таймаута.
# На боевом сервере переменная не задаётся — там действует true.
EXTERNAL_SERVICES=false

# Точечные переопределения, если нужно вернуть отдельный сервис на локале:
# EXTERNAL_GTM=true
# EXTERNAL_YANDEX_MAPS=true
# EXTERNAL_TOURVISOR_JS=true
# EXTERNAL_TRAVELPAYOUTS=true
# EXTERNAL_TRANSLATE=true
# EXTERNAL_CRM=true
# TinyMCE (cdn.tiny.cloud) под VPN доступен и включён по умолчанию,
# выключается явно: EXTERNAL_TINYMCE=false

LOG_CHANNEL=daily
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=mysql
DB_HOST=MariaDB-11.7
DB_PORT=3306
DB_DATABASE=vizitexpert.uz
DB_USERNAME=root
DB_PASSWORD=

BROADCAST_DRIVER=log
CACHE_DRIVER=file
FILESYSTEM_DISK=public
QUEUE_CONNECTION=database
SESSION_DRIVER=database
SESSION_LIFETIME=525600
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

MEMCACHED_HOST=127.0.0.1

REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379


MAIL_MAILER=smtp
MAIL_HOST=smtp.yandex.ru
MAIL_PORT=465
MAIL_USERNAME="info@hottour.com"
MAIL_ADMIN="it@hottour.com"
MAIL_PASSWORD="lqeatwxkvyzdoujk"
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="info@hottour.com"
MAIL_FROM_NAME="${APP_NAME}"
MAIL_ZAKAZ="zakaz@hottour.com"



AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

PUSHER_APP_ID=
PUSHER_APP_KEY=
PUSHER_APP_SECRET=
PUSHER_HOST=
PUSHER_PORT=443
PUSHER_SCHEME=https
PUSHER_APP_CLUSTER=mt1

VITE_APP_NAME="${APP_NAME}"
VITE_PUSHER_APP_KEY="${PUSHER_APP_KEY}"
VITE_PUSHER_HOST="${PUSHER_HOST}"
VITE_PUSHER_PORT="${PUSHER_PORT}"
VITE_PUSHER_SCHEME="${PUSHER_SCHEME}"
VITE_PUSHER_APP_CLUSTER="${PUSHER_APP_CLUSTER}"


DEBUGBAR_ENABLED=false
